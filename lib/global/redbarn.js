/**
 * Creates the redbarn namespace and any useful functions for managing
 * processors.
 *
 * @author Mike Atkisson
 * @since 0.1.0
 */
var redbarn = {
    processors: { }
};

module.exports = redbarn;